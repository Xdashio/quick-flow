node_modules/
dist/
!dashboard/.next/
!backend/dist/
out/
coverage/
.env
.env.local
.env*.local
data/*.db
data/*.db-*
*.log
.husky
.agents/
.DS_Store
tsconfig.build.tsbuildinfo
*.tsbuildinfo
**/*.db
**/*.db-*
**/dist/
!backend/dist/
!shared/dist/
!register/dist/
!register/dist/renderer/
!register/dist/renderer/**
# electron-builder output (installers are published via GitHub Releases, not git)
register/release/
# Register runtime image cache (downloaded per-terminal at sync time)
register/data/images/
# Dashboard production build is committed so HostPinnacle (no build step on
# shared hosting) can serve it directly after `git pull` + `npm install`.
# Only runtime files are tracked — build cache / dev / trace output is excluded:
dashboard/.next/cache/
dashboard/.next/dev/
dashboard/.next/trace
dashboard/.next/trace-build
dashboard/.next/diagnostics/
**/.agents/
skills-lock.jsonvenv/
virtual-printer
venv
skills-lock.json
dashboard/next-env.d.ts
backend/tmp/

